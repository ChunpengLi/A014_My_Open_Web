{
  "name": "firmware-manage-web-public",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-check.mjs && node --check src/worker.js && node --check public/app.js",
    "check": "npm run build",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
